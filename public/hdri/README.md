# public/hdri

Empty by design. Image-based lighting is baked at runtime from drei
`<Lightformer>` panels (day + night variants) — see `ASSETS_NEEDED.md` if you
want to swap in real `.hdr` / `.exr` environment maps.
