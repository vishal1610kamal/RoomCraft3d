# public/models

Empty by design. All furniture is generated from primitives at runtime — see
`ASSETS_NEEDED.md` for the exact filenames, dimensions, licence requirements
and the one-file swap path if you want to drop in real `.glb` models.
